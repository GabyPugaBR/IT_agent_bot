services:
  backend:
    build: ./backend
    container_name: constellations-backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: constellations-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
